# 🌐 IpFinder2

**IpFinder2** — консольное приложение, которое определяет страну и штат по введённому IP-адресу (IPv4 или IPv6).  
Реализовано с использованием принципов **трёхслойной архитектуры (DAL, BLL, Presentation)** для разделения логики, данных и взаимодействия с пользователем.

---

## ⚙️ Принцип работы

1. Программа загружает базу IP-диапазонов из `geo-US.csv`.
2. Пользователь вводит любой IP-адрес (v4 или v6).
3. Алгоритм находит диапазон, в который попадает IP, и возвращает:
   - **код страны**  
   - **название страны**  
   - **код штата**  
   - **название штата**
4. Если IP не найден — выводится сообщение  
   `Ip not found.`  
   Если формат IP неверный —  
   `Invalid IP address format.`

---

## 🧠 Особенности реализации

- Поддержка **IPv4** и **IPv6**
- Быстрый поиск по диапазону за счёт перевода IP в числовое значение
- Проверка корректности ввода IP
- Работа в **бесконечном цикле** до завершения программы пользователем

---

---

## 🧰 Требования

- [.NET 8 SDK](https://dotnet.microsoft.com/en-us/download/dotnet/8.0) или выше  
- CSV-файл с IP-данными (`geo-US.csv`) в корне проекта  

---

## 🚀 Запуск

```bash
dotnet run --project IpFinder2.Presentation
